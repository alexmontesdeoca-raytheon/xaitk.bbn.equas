version: '2'
services:
    equas-mongodb:
        image: mongo:3.6.3
        ports:
            - "27017:27017"
        command: mongos --configdb equas-mongodb-config
    equas-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    equas-mongodb-config:
        image: mongo:3.6.3
        container_name: equas-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
